<div class="index-action">
  <div class="instruments-index-title mt-4">
    <%# <div class="banner-index" style="background-image: url(https://res.cloudinary.com/dehuad3aa/image/upload/v1606402503/photo-1549401002-bcd2dba1a5c2_evhete.jpg); mr-10 ml-10"> %>
      <h1>Instruments  <i class='fas fa-music' style='font-size:55px; color: #e76f51'></i>
      <%# <i class="fas fa-guitar"></i> %>
      <%# <i class="fas fa-drum"></i> %>

      <br>
    </div>
  </div>
    <div class="map-container">

      <div>
        <% @instruments.each do |instrument| %>
          <div class="card-instrument-inline m-4">
            <%= cl_image_tag instrument.photo.key, width: 350, height: 310, crop: :fill %>
            <div class="card-instrument-inline-infos p-3">
              <h2> <%= instrument.name %> </h2>
              <h6> <span class="text-instrument-inline"> <%= instrument.description %> </span></h6>
              <div class="card-instrument-avatar d-flex align-items-end">
                <%= cl_image_tag instrument.owner.avatar.key, style: "border-radius: 50%; width:40px; height:40px;"%>
                <div class="d-flex justify-content-end mt-3">
                  <%= link_to instrument_path(instrument), class: "btn btn-primary" do %>
                  <%= instrument.price_per_day %> €/ jour
                <% end %>
                </div>
              </div>
            </div>
          </div>
        <% end %>
      </div>
      <div class="map-card-instruments">
        <div id="map"
        style="width: 100%; height: 780px;"
        data-markers="<%= @markers.to_json %>"
        data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>">
        </div>
      </div>
    </div>
</div>
